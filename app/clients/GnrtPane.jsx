const GNRTPane = () => {
  return (
    <p>
      Outright is the official brand partner for Generate, the AI event for a
      new era — where creation is as dynamic as the technology itself.
    </p>
  );
};

export default GNRTPane;
